{
  "name": "http-server",
  "version": "1.0.0",
  "description": "This is a server for pFractice.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon run start",
    "start": "nodemon run start",
    "watch": "node index.js",
    "migrate": "npx sequelize-cli db:migrate",
    "undo-migrate": "npx sequelize-cli db:migrate:undo",
    "seed": "npx sequelize-cli db:seed:all",
    "undo-seed": "npx sequelize-cli db:seed:undo"
  },
  "author": "Rehan Imran",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.5.2"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "axios": "^1.7.2",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.13",
    "pg": "^8.8.0",
    "pino": "^8.11.0",
    "sequelize": "^6.25.3",
    "yup": "^1.4.0"
  }
}
